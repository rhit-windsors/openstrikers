--- a/src/Game/FE/feChooseSideComponent.cpp
+++ b/src/Game/FE/feChooseSideComponent.cpp
@@ -131,7 +131,11 @@
             if (*playingSide != -1)
             {
                 mPlayerReady[i] = true;
-                ((TLInstance**)playingSide)[9]->m_bVisible = true;
+                // MWCC walked off &mPlayingSides[i] with a 4-byte stride:
+                // (this + i*4) + 9*4 lands on mInstanceTable[i + 4], which is
+                // what the matching "not ready any more" branch above hides.
+                // TLInstance* is 8 bytes here, so index the array directly.
+                mInstanceTable[(offset / 4) + 4]->m_bVisible = true;
                 TLInstance* ri3 = mInstanceTable[16];
                 if (ri3 != NULL)
                 {
--- a/src/Game/FE/feCaptainComponent.cpp
+++ b/src/Game/FE/feCaptainComponent.cpp
@@ -24,14 +24,16 @@
 {
     int j;
     int i;
-    IChooseCaptain* col;
-    IChooseCaptain* row;
 
-    for (i = 0, row = this; i < 2; i++, row = (IChooseCaptain*)((u8*)row + 0xC))
+    // MWCC walked mAsyncImage by hand: a row was 0xC bytes (three 4-byte
+    // pointers) and a column 4, both read through mAsyncImage[0][0]. The
+    // strides are 0x18 and 8 here, so index the array instead of stepping
+    // "this" -- otherwise these deletes free whatever follows the array.
+    for (i = 0; i < 2; i++)
     {
-        for (j = 0, col = row; j < 3; j++, col = (IChooseCaptain*)((u8*)col + 4))
+        for (j = 0; j < 3; j++)
         {
-            delete col->mAsyncImage[0][0];
+            delete mAsyncImage[i][j];
         }
     }
 
